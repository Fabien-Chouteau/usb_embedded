with "config/tests_config.gpr";
with "config/tests_list_config.gpr";
project Tests is

   for Source_Dirs use ("src/", "config/", "support/");
   for Object_Dir use "obj/" & Tests_Config.Build_Profile;
   for Create_Missing_Dirs use "True";
   for Exec_Dir use "bin";
   for Main use Tests_List_Config.Test_Files;

   package Compiler is
      for Default_Switches ("Ada") use Tests_Config.Ada_Compiler_Switches &
      ("-gnatyM200",
       "-gnata");
   end Compiler;

   package Binder is
      for Switches ("Ada") use ("-Es"); --  Symbolic traceback
   end Binder;

   package Install is
      for Artifacts (".") use ("share");
   end Install;

end Tests;
